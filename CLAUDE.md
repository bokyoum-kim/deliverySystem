# 물류관리 시스템 (Coupang Vendor Fulfillment)

쿠팡 로켓배송 공급사(문구 유통)를 위한 발주 → 분류 → 패킹 → 재고 관리 시스템.
현재 단일 HTML 프로토타입이 동작 중이며, 이를 실제 제품으로 확장하는 단계.

---

## 1. 해결하려는 업무

쿠팡에서 받은 발주서(엑셀)를 물류센터(FC)별로 구분해, 상품을 박스에 나눠 담은
Packing List를 만들고, 출고분만큼 재고를 차감한다. 재고가 부족하면 구매발주를 내고,
단종 상품은 반송 처리한다.

---

## 2. 데이터 모델 (마스터 워크북: `문류관리시스템_IDEA.xlsx`)

이 워크북의 시트 구조가 마스터 스키마의 기준이다.

| 시트 | 내용 |
|---|---|
| `ProductInfo` | 상품번호(PK), 바코드, 상품명, 포장수량, 무게(g), 가로/세로/높이(mm), 가격 |
| `InStock` | 상품번호, 기초수량 (= 재고) |
| `HoldItem` | 단종 상품번호 목록 → 반송 대상 판정에 사용 |
| `Destination` | 통합지역, 지역, 명칭(=FC명), 주소 |
| `Box` | 상자종류, 가로/세로/높이(mm), Hold 가능 무게(g), 보유수량(개) |
| `Order` | 발주번호, 물류센터, 상품번호, 상품명, 확정수량 |
| `PurchseOrderList` | 구매발주 목록 (부족분 발주용, 원본 철자 그대로) |

### ERD (12 엔티티, 발주라인이 허브)

- `상품마스터`(상품번호 PK, 크기, 무게, 판매상태) ─1:1─ `재고`(가용수량)
- `상품마스터` ─1:N─ `구매발주`(수량, 상태, 입고수량)
- `물류센터`(FC코드 PK) ─1:N─ `발주서`(발주번호 PK, 입고예정일, 회송지주소)
- `발주서` ─1:N─ **`발주라인`**(라인ID PK, 발주수량, 확정수량, 처리상태, 사유코드)
- `사유코드`(처리유형) ─1:N─ `발주라인`
- `발주서` ─1:N─ `박스` / `팔레트`, `박스규격`·`팔레트규격` ─1:N─ 각각
- `박스` ─1:N─ `박스내역`(상품번호, 수량)

`발주라인.처리상태`(출고/발주/반송) 한 컬럼이 전체 흐름을 분기시킨다.
반송은 별도 테이블 없이 `처리상태=반송` + 사유코드로 표현.

---

## 3. 비즈니스 규칙 (반드시 유지)

### 분류 (라인별 3분기)
1. 상품이 `HoldItem`(단종)에 있으면 → **반송**
2. 재고 부족 → **발주**(보충) — 단, 패킹 리스트에서 제외하지 않음(아래 참조)
3. 그 외 → **출고**

### 포장수량 (2026-08 추가)
- 상품마스터에 등록된 가로/세로/높이/무게는 **개별 단품이 아니라 `포장수량`개들이 묶인 포장 1개** 기준값이다.
  (모든 등록 상품은 포장수량만큼 포장된 상태의 크기·중량으로 등록되어 있다는 전제.)
- 발주서는 여전히 **개별 상품 수량**으로 들어온다 (포장 단위로 오지 않음).
- 패킹 시 부피·무게는 **포장수량의 배수(완전한 포장 묶음)만큼만** 박스 용적/무게에 반영한다.
  포장수량으로 나누어떨어지지 않는 나머지(단수)는 **그 박스에 부피·무게를 차지하지 않는 것으로 보고**
  그대로 얹어서 함께 포장한다 — 별도 박스를 새로 열지 않는다.
  예) 포장수량 10인 상품을 25개 발주 → 2포장(20개) 분량만 용적/무게 계산에 반영, 나머지 5개는 공짜로 동봉.
- 상품이 상품정보에 없어 즉석 등록되는 경우 포장수량은 기본값 1(개별 포장 없음)로 등록된다.

### 패킹 (혼적 = 여러 상품을 한 박스에)
- 박스당 적재수 = **부피 · 무게 · 수량 중 가장 먼저 차는 기준**
  - 부피: `박스 L×W×H × 충진율(기본 0.8)` vs 상품 부피 누적(포장수량 배수 기준, 위 참조)
  - 무게: 박스의 `Hold 가능 무게(g)` vs 상품 무게 누적(포장수량 배수 기준, 위 참조)
  - 수량: 박스당 최대 수량(기본 100) — **포장(팩) 개수 기준**(개별 낱개 기준 아님). 부피·무게와 마찬가지로
    포장수량 단위로 세며, 나머지(단수)는 이 수량에도 포함하지 않는다.
    (초기에는 개별 낱개 기준으로 셌으나, 포장수량이 큰 상품에서 박스 용적·무게 여유가 남는데도
    수량 캡이 먼저 걸려 박스가 지나치게 일찍 나뉘는 버그가 있어 2026-08에 수정됨.)
- **여러 박스 종류 혼용**: 큰 박스부터 채우고, 그 종류 재고가 떨어지면 다음 크기로.
  각 배송지의 **마지막 잔량 박스는 들어가는 가장 작은 박스로 다운사이즈**.
- 그룹핑 키: 배송지(FC). 쿠팡 발주는 발주번호 1건 = FC 1곳(1:1)이라 발주번호 단위도 동일.
- (초기 규칙) 12박스 초과 시 팔레트 전환 — 현재 HTML 버전에는 미구현, 백로그.

#### 발주번호 혼적 예외 (2026-08 추가)
기본은 **한 박스에는 하나의 발주번호 물품만** 담는다. 아래 두 경우만 예외로 다른 발주번호와
섞이거나 박스 정원을 넘긴다(둘 다 기준값은 **포장(팩) 단위 10묶음**):
1. **꼬리 잔량 오버플로우**: 한 발주번호를 박스에 채우다가 박스가 가득 차서 새 박스를 열어야
   하는 시점에, 그 발주번호의 남은 물량(현재 박스 이후 전부)이 **10묶음 이내**면 새 박스를 열지
   않고 지금 박스에 정원을 넘겨서라도 그대로 얹는다.
2. **소량 발주 병합**: 한 발주번호의 전체 물량이 **10묶음 미만**이면 전용 박스를 새로 열지 않고,
   같은 배송지(FC)로 가는 **다른 발주번호의 박스 중 여유(부피·무게·수량 모두 남는 정도)가 가장
   많은 박스**에 합쳐 담는다 — 이 경우 한 박스에 발주번호가 2개 이상 들어갈 수 있다. 합칠 수
   있는 박스가 없으면(모두 꽉 찼으면) 기존처럼 전용 박스를 새로 연다.

구현: `web/src/lib/packing.ts`의 `packDest()` — `SMALL_ORDER_PACK_LIMIT`(예외 2),
`OVERFLOW_TAIL_PACK_LIMIT`(예외 1) 상수.

### 재고
- **패킹 리스트는 재고와 무관하게 주문 전량으로 작성한다.** (중요: 이전 버전 버그)
- 재고는 **"출고 확정" 시점에** 가능한 만큼 차감(0 미만으로 내려가지 않음).
- 부족분은 구매발주 목록으로 이동 → 입고 등록 시 재고 증가.
- 박스 재고도 **종류별로** 차감/복원.

### 단위
- 크기 mm, 무게 g, 금액 원. 부피 계산 시 mm³ → cm³ 환산은 /1000.

---

## 4. 현재 구현 상태

`물류관리시스템.html` — 단일 파일 프로토타입 (바닐라 JS + SheetJS CDN)

**동작하는 것**
- 첫 화면 = 재고 현황(총 재고수량/재고 가치/단종 품목/박스 보유)
- 상품·박스·배송지 등록/수정 화면
- 마스터 엑셀 업로드(IDEA 워크북 시트 자동 인식)
- Order 엑셀 업로드 + 컬럼 자동 인식(`사용자 지정.` 접두어 제거, 발주번호 없는 하단 코드표 행 제외)
- 배송지별 Packing List 생성(박스 종류 태그, 박스별 수량·중량)
- 출고 확정 → 상품·박스 재고 차감 / 되돌리기
- 엑셀 내보내기: 배송지요약 · 박스요약 · PackingList · 재고부족 · 반송
- 반송 리스트 별도 엑셀

**한계 (여기서부터 작업)**
- 브라우저 메모리만 사용 → 새로고침하면 초기화. 영속성 없음
- 백엔드/DB 없음, 사용자·권한 없음
- 팔레트 로직 미구현
- 패킹은 부피 근사(충진율). 실제 3D 배치 아님

---

## 5. 다음 단계 (우선순위)

1. **영속성**: DB 스키마(위 ERD 기준) + API. 마스터/주문/패킹 결과 저장
2. **프레임워크 이전**: 단일 HTML → React 등. 화면 단위 컴포넌트 분리
3. **팔레트**: 배송지당 박스 12개 초과 시 팔레트 묶음 + 팔레트 라벨
4. **정밀 패킹**: 3D 빈패킹 휴리스틱(Extreme Point / DBL)은 연산이 무거우므로 백엔드에서
5. **출력물**: 박스별 패킹 슬립 PDF(바코드 포함), FC별 일괄 출력
6. **멀티테넌트**: 여러 공급사 사용 시 `테넌트` 엔티티 최상위 추가

---

## 6. 작업 규칙

- UI 문구·주석·커밋 메시지는 한국어
- 금액/수량은 천 단위 구분, 숫자는 고정폭(monospace) 표기
- 상태 색상: 출고=녹색(#157F5C), 발주/경고=주황(#A8690C), 반송/오류=빨강(#B93B2D)
- 엑셀 컬럼명은 IDEA 워크북 원본 표기를 따른다(`PurchseOrderList` 철자 포함)
- 기존 패킹 로직을 바꿀 때는 위 "비즈니스 규칙"을 먼저 확인할 것
